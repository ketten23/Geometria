package com.example.geometria;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.TextView;

public class areatriangulo extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_areatriangulo);

        TextView txtidtriangulo = (TextView) findViewById(R.id.idResultadotri);
        EditText editText = (EditText) findViewById(R.id.idaltura);
        EditText editTextbase = (EditText) findViewById(R.id.idbase);


        Button btncalulartriangulo = (Button) findViewById(R.id.idcalculartrian);

        btncalulartriangulo.setOnClickListener(new View.OnClickListener){


            }
        }
    }
